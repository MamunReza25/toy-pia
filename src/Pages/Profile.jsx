import React from 'react';

const Profile = () => {
    // updateUserProfile(name, photo)
    // 
    return (
        <div>
            <title>profile-update</title>
            profile
        </div>
    );
};

export default Profile;